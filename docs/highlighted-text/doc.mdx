import { Demo } from './demo'
import { Demo as DemoDirection } from './demo-direction'

<DemoCanvas>
  <DemoPreview>
    <Demo />
  </DemoPreview>
  <DemoCode>
    ```tsx file=./demo.tsx
    ```
  </DemoCode>
</DemoCanvas>

## Installation

<InstallationTabs item="highlighted-text">
```tsx file=../../registry/spell-ui/highlighted-text.tsx
```
</InstallationTabs>

## Examples

### Direction

<DemoCanvas>
  <DemoPreview>
    <DemoDirection />
  </DemoPreview>
  <DemoCode>
    ```tsx file=./demo-direction.tsx
    ```
  </DemoCode>
</DemoCanvas>

## Usage

```tsx
import { HighlightedText } from "@/components/highlighted-text";
```

```tsx
<HighlightedText>
  Your text here
</HighlightedText>
```

## Props

<PropsTable
  data={[
    {
      name: "children",
      type: "React.ReactNode",
      required: true,
      nameDetails: "The text content to highlight"
    },
    {
      name: "from",
      type: "'left' | 'right' | 'top' | 'bottom'",
      default: "'bottom'",
      nameDetails: "Direction from which the background slides in"
    },
    {
      name: "delay",
      type: "number",
      default: "0",
      nameDetails: "Animation delay in seconds"
    },
    {
      name: "inView",
      type: "boolean",
      default: "false",
      nameDetails: "Whether to trigger animation only when element is in viewport"
    },
    {
      name: "className",
      type: "string",
      nameDetails: "Additional CSS classes to apply"
    }
  ]}
/>